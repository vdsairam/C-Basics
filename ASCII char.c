#include<stdio.h>
void main()
{
	int n=67, c=120;
	char ch='Z', c_h='S';
	printf("Corresponding ASCII character for %d is %c\n",n,n);
	printf("Corresponding ASCII character for %d is %c\n",c,c);
	printf("\nCorresponding ASCII number for %c is %d\n",ch,ch);
	printf("Corresponding ASCII number for %c is %d",c_h,c_h);
}
